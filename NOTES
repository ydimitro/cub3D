unused or other problems according CLION plugins

# define MAX_MAP_LINES 100
# define KEY_Q 12
# define KEY_UP
# define KEY_DOWN

s_texture_calc 
- tex_y
- color

s_texture
- sl
- end

s_player
- dir_x
- dir_y
- speed
- turn_speed

s_data
- game_map_capacity
- in_map_section
- ceiling_color
- floor_color[3];
- *img_data;
- bits_per_pixel;
- size_line;
- endian;

//destroy.c
void    cleanup_texture(t_data data);

//direction.c
int        set_player_start(t_datadata, char orientation, int x, int y);

//drawing_utils_2.c
void    calc_line_dir(t_data data, t_lineline);

//drawing_utils_texture.c
void    set_texture(t_data data, t_texture **texture);

//map_get_valid.c
int        is_map_valid(t_datadata);

